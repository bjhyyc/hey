# Hey Pet 正式上线前检查清单

更新时间:2026-09-02(定价、客服、法务、下载地址已落地;剩余项见「二」)

## 当前结论

技术链路已全部打通并在生产运行:production-assured Worker(含视频人工放行)、
管理台八项处置、预检门禁、限速、审计、唯一管理员、每日备份与 10 分钟巡检。
正式定价(¥48)已在生产数据库生效,客服入口、法务四页、下载地址已烘焙进 r46。
**现在离公开收费上线只差三个动作:部署 r46、上传安装包、Kaipay 真实支付验收。**

## 一、已完成并在生产运行 ✅

- [x] production-assured Worker(`admin-override-20260827`,manifest `c0df69f1…` 与生产钉值逐字节一致);API `seam-fixes-20260826`;迁移 001–021 全部应用。
- [x] 真实批次验收:v2prompt 边牧批次 3 母图 + 7 动作全过生产 QA 门、打包、原版导入、真实 Electron 七项交互(08-18);此后多个真实客户单成品交付(匡匡/嘎嘎嘎/7777/uuu 等 deliverable)。
- [x] 支付全链:Kaipay V3 下单→回调验签→主动查单→paid→工作流启动一次,19 笔 0.01 元真实订单跑通;同步回跳页;webhook 延迟时服务端查单兜底。
- [x] 照片预检门禁(`ENFORCED=true`,vision 45s 超时,网关 60s 预算,体积漏斗 16→10→8MB)。
- [x] 管理台:检索/详情/重跑/人工放行(母图+睡姿+视频)/补发重生成/交付重开/退款(暗启动)/账号封禁,全部必填原因写 `audit_event`;唯一管理员已任命(2658b6e3…,任命脚本强制唯一性)。
- [x] 跨层接缝清查 8 项修复(超时表、限速身份见证、错误文案分层、幂等键、照片替换降采样、边缘配置对齐并合同测试锁死)。
- [x] 每日 PostgreSQL 备份 cron(19:30 UTC,保留 7 天,已试跑并 pg_restore 校验)+ 每 10 分钟运维巡检 cron(落 `/opt/petpack/shared/logs/`)。
- [x] 客户端:Hey 1.0.0(appId `com.heyirmy.hey`),安装/启动/卸载实测通过,SHA `b2697de6…c600`。
- [x] **正式定价 ¥48**(09-02):生产 `product_plan` 插入 `hey-petpack-standard-48-20260902`(4800 分,enabled),0.01 元冒烟计划已 `disabled`;前端默认 plan code 已同步(r39)。
- [x] **客服入口 `/support`**(09-02):QQ 3501551445 烘焙;页脚/失败卡/隐私/协议四处统一指向该页;唤起按钮按设备选协议;群链接与名片链接为可选构建参数,未配置则不渲染。
- [x] **法务三页**(09-02):`/privacy` 全文、`/ai-content` 生成式 AI 显式标识、`/terms` 补齐协议双方/服务期限/账号规范/联系/法律适用;页脚四链接(含联系客服);下单勾选文案链到协议与 AI 说明。所有条款只陈述系统真实行为。
- [x] `/how-it-works` 制作流程页(5 步 + 谁做/耗时,首页上传卡下方入口)。
- [x] 下载地址烘焙:`https://hey-download-1462360313.cos.ap-shanghai.myqcloud.com/client/Hey-Setup-1.0.0.exe` + SHA + 版本(r39)。

## 二、上线测试进行中(2026-09-02)

**当前生产状态(我已设置好,可直接开测)**
- 价格:`hey-petpack-standard-48-20260902` 临时改为 **10 分(¥0.1)**,名称
  「Hey 素材包 标准版(上线测试价)」。改价只动数据库,**不需要重新部署**;
  测完一条 SQL 就能改回 4800。
- **管理员退款已开启**:`PETPACK_ADMIN_REFUND_ENABLED=true`,API 容器已重建并 healthy
  (原 compose 备份在 `compose.yaml.bak-20260902`)。退款按钮现在是真的会打钱。
- 七个容器全 healthy;队列无失败作业(sent=1332);预检门禁开、销售开、限速开。
- ⚠️ 站点是公开的:测试期间任何人都能以 ¥0.1 下单。测完尽快改回。

**测试基线(用来区分测试产物与历史数据)**
- run:deliverable=6、failed=6、awaiting_character_confirmation=1、video_generating=1
  (后两条分别停了 14 天和 13 天,是历史卡单,不是本次测试产生的)
- order:paid=14、pending_payment=5

**测试顺序**
1. 上传 `petpack-studio-web-r47.zip` 到 CloudBase 并部署。
   ⚠️ 线上现在还是 r37,烘焙的是**已停用的冒烟计划 code**,不部署 r47 下不了单。
2. 上传安装包:用**重建后的** `dist/Hey-Setup-1.0.0.exe`
   (SHA `b2697de6…c600`),旧包哈希与站点展示值已对不上。桶与对象键见
   `docs/CLIENT_DISTRIBUTION.md`。
3. 全流程:预检 → 下单 ¥0.1 → 支付宝支付 → 母图确认(试一次重生成)→
   七段动作 → 下载素材包 → 客户端导入并验证七项交互。
4. Kaipay 退款验收:管理台对该单发起退款 → 观察 refund_pending → 轮询收敛为 refunded
   → 支付宝端确认到账。
5. 管理员处置验收:重跑、人工放行、交付重开、账号封禁各试一次(都会写审计)。
6. 测试通过后告诉我,我执行「全面开放」:价格改回 ¥48、清理两条历史卡单、
   确认队列与巡检,退款保持开启。

## 三、上线前对账## 三、上线前对账(我来做,不需要用户)

- [ ] `execution_dead=11`、`queue_failed=5` 历史基线对账:逐条归类
  (已修缺陷产物 / 预算耗尽正当死亡 / 待救援单),清理或调巡检阈值,
  让告警从此只报增量。
- [ ] 6 个 failed 真实单处置:柯基、222×2、11111、1111 用管理台逐单
  救援或(退款验收后)退款,天体吧/哈哈/333 三个未支付单可过期清理。
- [ ] 留存清理从 `dry-run` 切到真实执行(隐私政策承诺"超过服务期后分批清理",
  目前靠手动;切换前先在生产跑一次 dry-run 报告核对候选集)。
- [ ] 上线当日:确认队列零积压、7 容器 healthy、备份最新、巡检绿。

## 四、上线后第一周观察项

- 巡检日志 ALERT 行(现在会自动落盘);预检 429/超时率;
  Kaipay 回调延迟触发查单兜底的频率;COS 用量与成本(含下载桶流量)。
- 边缘容量型限速(xcaddy rate-limit 插件或腾讯云 WAF)仍是部署侧待办,
  当前依赖应用层每 IP 300/12 rpm。

## 禁止事项(沿用)

- 不单独重复生成已完成的动作;整批七动作原则不变。
- 不把 `/readyz=200` 当作媒体生产链路已放行。
- 不在日志、报告或测试输出中写出 API key、支付密钥、签名原文或私有 URL。
- r46 部署前不做任何推广拉新(线上仍烘焙冒烟 code,下单会被拒)。
