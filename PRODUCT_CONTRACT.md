# Hey / PetPack Studio 当前产品合同

> 状态：当前权威产品合同  
> 原始任务源：`C:\Users\86135\.codex\attachments\e41448ab-fd7d-491b-b9c4-4463f393e7d2\pasted-text.txt`  
> 裁定原则：同一事项发生冲突时，以本线程中时间更晚、表达明确的用户裁定为准；未被覆盖的内容继续沿用原任务源。

## 1. 产品定义

- 对外品牌：`Hey`。
- 首页主文案：`Hey, I really miss you.`。
- `PetPack Studio` 仅作为内部项目/服务名称。
- 核心商品是可直接导入客户端的 `.petpack`；客户端是承载 PetPack 的工具，不是主要收费商品。
- 面向中国的猫、狗宠物主人。
- 图片与视频生成只使用火山引擎 ModelArk：Seedream 5.0 Pro、Seedance 2.0。
- 正式视频规格为 480p（854×480），不再以 720p 为默认交付规格。
- 客户端只基于干净的 `duzexu/desktop-pet` 上游，不使用、不迁移、不参考 `dyberpet-dahui`。

## 2. 最终用户流程

1. 首页选择猫或狗。
2. 上传最少 3 张、最多 4 张照片：
   - 两张清晰正面全身照，必填；
   - 一张约 45° 侧面全身照，必填；
   - 第二张约 45° 侧面全身照，可选。
3. 每张照片可以单独添加、替换和删除；相同或高度近似照片不能重复占槽。
4. 满足最低数量后点击视觉常黑的“开始制作”。
5. 未登录用户使用 CloudBase 手机号短信验证码直接登录，无需注册或密码。
6. 用户选择套餐并通过支付宝完成付款。
7. Seedream 生成两张清醒母图：严格正面和约 45° 侧面。
8. 两张母图分别确认，可分别重生成；每张为首次生成加最多 2 次用户主动重生成，系统失败重试不占次数。
9. 两张均确认后，用户只点击一次“确认形象，开始制作”。
10. 系统自动生成睡着母图，不增加用户点击或确认。
11. 系统一次性并发提交 7 个 Seedance 视频任务。
12. 云端自动抠图、去绿、毛发边缘处理、尺度/位置校正、循环检查和质量检测。
13. 自动构建并用兼容客户端验证 `.petpack`。
14. 用户下载 PetPack；“下载客户端”在新标签页打开。

支付成功前不得创建真实收费的 Seedream 或 Seedance 任务。首页照片可本地预览，但未登录、未付款时不永久上传。

## 3. 输入体验原则

- 不把用户困在第一步。
- 仅在必填槽位缺失、文件损坏/不支持、安全风险、高度重复、完全无法识别宠物或主体严重缺失时硬阻止。
- 清晰度、角度、遮挡和光线优先给简短建议，不用苛刻分数机械拒绝。
- 按钮始终保持用户指定的常黑视觉；条件不足时点击后明确提示缺少哪个槽位，不进入下一页。
- 猫、狗共用动作体系，以物种变量和身份描述区分，不维护两套业务流程。

## 4. 母图和角色锁定

- `awake-front`：严格正面。
- `awake-three-quarter`：约 45°，不得生成完全 90° 侧面。
- 两张清醒母图共同锁定头部轮廓、五官、耳朵、身体比例、毛发质感、花色边界、尾巴和独有斑纹。
- 风格使用“高质量、友好、统一的 3D 动画宠物风格”，不得直接使用其他公司的动画品牌名称。
- 用户可分别重生成两张清醒母图；内部失败不消耗用户权益。
- 超过用户重生成上限仍无法交付时进入内部复核、人工补救或退款，不要求用户不断充值。
- 睡着母图在两张清醒母图确认后自动生成，同时引用两张母图身份；不要求用户确认。

## 5. 统一画布与后处理

- 统一比例 16:9，最终视频 854×480。
- 图片模型可生成更高分辨率原图，但送入 Seedance 前和交付时统一到相同画布。
- 宠物完整可见，不裁切耳朵、尾巴或爪子。
- 跨姿态尺度不能只比较外接框高度，应结合头部、躯干、肩部和地面基线。
- 提示词用于提高稳定性；位置、大小和边缘质量必须由后处理与质检最终保证。
- 处理链至少包括主体分割、绿幕抠图、去绿色溢出、浅色毛发保护、黑边消除、半透明毛发细化、Alpha 一致性、逐帧尺度/基线/偏移校正、首尾检查和循环检查。
- 上线必须解决整块绿背景、毛发染绿、白底黑边、尺寸跳变、位置跳变和透明空白区域阻挡鼠标。

## 6. 七动作合同

| Action ID | 用途 | 锚点 |
|---|---|---|
| `idle` | 默认待机循环 | 清醒 → 清醒 |
| `sneeze` | 左键单击打喷嚏 | 清醒 → 清醒 |
| `roll` | 左键双击打滚 | 清醒 → 清醒 |
| `sleep-transition` | 一次性进入睡眠 | 清醒 → 睡着 |
| `sleep-loop` | 睡眠循环 | 睡着 → 同一睡着状态 |
| `stretch` | 启动和唤醒时伸懒腰并打哈欠 | 睡着/清醒 → 清醒 |
| `hover-attention` | 有效区域停留 2 秒后的关注动作 | 清醒 → 清醒 |

- 七任务并发提交，不串行逐个生成。
- 无音频、水印、文字、人物、其他动物、道具、奔跑、随机走动或掉落物。
- 两张已确认母图共同作为身份参考；供应商不支持多参考时使用由两张母图构建的身份参考板。
- 运动中的轻微花色波动可容忍；明显换色、结构改变、首尾身份错误必须失败。
- 修正旧提示词中的 `@long`、`@sleep` 引用错误。

## 7. 提示词策略

- 默认采用“老版提示词稳定版”：保留老版更可爱、生动的动作核心，只追加稳定性约束。
- 稳定性约束包括固定镜头、禁止运镜和变焦、禁止缩放/变形/漂移、锁定两张母图身份和花色、无地面阴影、无绿色染色、固定地面接触点和首尾一致。
- `idle` 动作较小，不持续兴奋。
- `sleep-loop` 仅缓慢、轻微、规则呼吸；一个循环包含完整呼吸周期，首尾同相位；禁止急促或剧烈胸腹起伏。
- `stretch` 必须包含自然打哈欠。
- 七套提示词由管理员维护，支持草稿、发布、停用、复制和回滚；普通用户不可见。
- 全部七条已发布后才允许生产订单进入视频生成；订单冻结提示词版本和哈希。

## 8. 客户端合同

- 首页/首次使用界面最显眼位置提供“导入我的 PetPack”，支持点击选择和拖拽导入。
- 导入前检查 manifest、资源、路径、编码、动作映射和版本；成功后自动激活并显示宠物。
- 设置、替换 PetPack 和退出保留在托盘/菜单，不占首页主区域；右键是动作，不能作为设置或退出的唯一入口。
- 启动：`stretch`（伸懒腰 + 哈欠）后进入 `idle`。
- 单击：`sneeze`。
- 双击：`roll`，双击判定完成前不得先播放单击。
- 右键：`stretch`；睡眠时先唤醒再播放。
- 连续 22 秒无交互：一次性 `sleep-transition`，结束后循环 `sleep-loop`。
- 鼠标在有效主体区域停留超过 2 秒：`hover-attention`；同一次停留仅一次，离开后重置，15–30 秒冷却，仅待机触发。
- 优先级：单击/双击/右键 > 睡眠切换 > 悬停 > 待机。
- 宠物外部透明区域鼠标穿透；命中区域跟随非透明主体，不能使用整块视频矩形永久拦截鼠标。

## 9. 网站体验合同

- 中文优先，同时保持英文品牌拼写正确。
- 左上角 `Hey`，无句点。
- 上传区标题 `Hey, I really miss you.` 使用祖母绿色并加粗，字号克制、圆润、精致。
- 使用简洁现代无衬线字体，不使用手写体。
- 首页不放右下角互动宠物。
- 每张照片有独立缩略图和删除入口。
- 模型标签靠右：`Seedream 5.0 Pro`、`Seedance 2.0`，可使用猫爪/狗爪图标。
- 次级页面与主页保持相同字体、颜色、留白和右上角导航；避免粗大标题和多余说明。
- 角色确认页不展示预置的具体猫狗形象。
- 支持桌面和移动端。
- 页脚只显示备案号 `蜀ICP备2026044501号`，链接工信部。
- 旧“两张图片”文案必须改成符合“两张正面 + 一至两张 45° 侧面”的短文案。

## 10. 登录、支付与云服务

- 登录：腾讯云 CloudBase 手机短信验证码，无密码、无需注册；必须有服务端限流、防刷、刷新保持和主动退出。
- 支付：支付宝开放平台“电脑网站支付”，公钥模式；开发环境只允许模拟支付。
- 营业执照、商户入驻和电脑网站支付审批完成前，不开放真实购买。
- 支付成功由服务端异步通知验签与主动查询共同确认；跳转页不代表成功；订单、通知、查询、发货、补发和退款必须幂等。
- 微信支付暂缓，不接易宝、易支付、个人收款码或资质不明平台。
- 域名：`heyirmy.com`；备案：`蜀ICP备2026044501号`。
- CloudBase 环境：`petpack-staging-d3ffsbun2dc89fae`；云托管服务：`petpack-studio-web`。
- COS：`heyirmy-petpack-staging-1462360313`，`ap-shanghai`；生产对象私有并使用限时签名 URL。
- Lighthouse：Ubuntu 24.04 / Docker 29 / 4C8G / 120GB / 10Mbps / `106.54.227.218`，承担 FFmpeg、抠图、质检、打包和兼容验证 Worker。
- PostgreSQL 和 Redis/BullMQ 的生产实例、备份和网络仍需重新盘点，不能把本地安装当作生产资源。

## 11. 付费结果承诺

- 用户购买的是完整、可导入、可使用的 PetPack，不是若干次不保证成功的模型调用。
- 模型失败和内部重试不消耗用户权益，不要求追加付款。
- 不向用户逐条展示需要自行判断的失败视频。
- 达到内部成本上限仍无法交付时进入人工处理或退款。
- 不得交付缺动作、无法导入、明显跳变、严重绿边或黑边的 PetPack。

## 12. 上线硬门禁

- CloudBase 短信登录通过发送、错误码、刷新、退出、服务端限流和防刷测试。
- 营业执照、支付宝商户和电脑网站支付审批完成；公钥验签、回调、查询、幂等、退款完成小额真实验收。
- 猫狗、明显花色、3 张和 4 张组合均完成真实生成验收。
- 两张母图角度、独立重生成、自动睡姿和七个 480p 并发任务全部通过。
- 老版提示词稳定版七条发布，`@long`/`@sleep` 修正并冻结版本。
- 去绿、浅色毛发、白底黑边、尺度/落点、睡眠呼吸与循环接缝通过。
- PetPack 真实导入；单击、双击、右键、22 秒入睡、睡眠循环、悬停 2 秒和透明区域穿透均实测。
- 生产 PostgreSQL、Redis/队列、备份、COS 权限、密钥、Worker 恢复、限流、死信、人工补偿、数据清理和监控就绪。
- 隐私政策、用户协议、退款规则和 AI 内容说明上线。
- Windows 安装包、版本追踪、多显示器和常用缩放比例验收通过。

## 13. 不得回退的历史覆盖

- 两张照片 → 最少 3 张、最多 4 张。
- 单张清醒母图 → 正面和 45° 两张清醒母图。
- 720p → 480p（854×480）。
- 30 秒睡眠 → 22 秒。
- 易宝 YOP → 支付宝开放平台电脑网站支付、公钥模式。
- 新版平淡提示词 → 老版动作核心 + 稳定性约束。
- 串行生成 → 七视频并发提交。
- 睡眠循环曾删除 → 最终保留 `sleep-loop`。

